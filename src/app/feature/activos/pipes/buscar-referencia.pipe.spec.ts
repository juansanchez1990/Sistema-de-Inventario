import { BuscarReferenciaPipe } from './buscar-referencia.pipe';

describe('BuscarReferenciaPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarReferenciaPipe();
    expect(pipe).toBeTruthy();
  });
});
