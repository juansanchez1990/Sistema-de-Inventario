import { BuscarDescripcionPipe } from './buscar-descripcion.pipe';

describe('BuscarDescripcionPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarDescripcionPipe();
    expect(pipe).toBeTruthy();
  });
});
