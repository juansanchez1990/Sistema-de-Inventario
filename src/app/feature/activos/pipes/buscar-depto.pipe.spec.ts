import { BuscarDeptoPipe } from './buscar-depto.pipe';

describe('BuscarDeptoPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarDeptoPipe();
    expect(pipe).toBeTruthy();
  });
});
