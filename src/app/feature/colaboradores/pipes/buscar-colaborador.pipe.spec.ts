import { BuscarColaboradorPipe } from './buscar-colaborador.pipe';

describe('BuscarColaboradorPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarColaboradorPipe();
    expect(pipe).toBeTruthy();
  });
});
